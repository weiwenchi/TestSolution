@model DateTime?
@{
 DateTime dt = DateTime.Now;
 if (Model != null)
 {
     dt = (System.DateTime)Model;

 }
@Html.TextBox("", String.Format("{0:yyyy-MM-dd}", dt.ToString("yyyy-MM-dd")),
  new { @class = "datefield", type = "date"})
}